import React from 'react';

// useEffect() will fetch Cart items for given userId from the database and save them in state
// Retrieve user information from cookies/local-storage
// cart.length === 0 && 'your cart is empty'

export const Cart = () => {
  return (
    <>
      <div>Cart items</div>
    </>
  )
}